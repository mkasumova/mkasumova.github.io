function toggleItem(mustache) {
   
    let item = document.getElementById(mustache);
   
    item.style.display = (item.style.display === "none" || item.style.display === "") ? "block" : "none";
}

function toggleItem(wig) {
   
    let item = document.getElementById(wig);
   
    item.style.display = (item.style.display === "none" || item.style.display === "") ? "block" : "none";
}